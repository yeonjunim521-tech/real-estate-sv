{
  "name": "real-estate-sv",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "wrangler types && tsc --noEmit",
    "check:frontend": "node --check site/main.js",
    "generate:og": "node scripts/generate-og.mjs",
    "build": "wrangler deploy --dry-run --outdir .wrangler-dist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "sharp": "^0.35.3",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.110.0"
  }
}
